## 1. Implementation
- [ ] 1.1 Update `src/components/Layout.tsx` to conditionally render navigation items based on the user's role (`artist`, `contributor`, `provider`, `admin`).
- [ ] 1.2 Create `src/pages/admin/RoleManagement.tsx` page to display roles (`user`, `artist`, `contributor`, `provider`) and allow updating their granular permissions.
- [ ] 1.3 Add "Roles & Permissions" to the admin navigation in `Layout.tsx`.
- [ ] 1.4 Wire up the RoleManagement page to a backend service (or create mock functions) for fetching and updating permissions.
